language: node_js
node_js:
 - "stable"
cache:
  directories:
    - node_modules
script:
  - npm run test
  - npm run build
deploy: 
  provider: heroku
  app: keystone-blog
  project: ./dist/
on:
  branch: master